# IO

### Every module should be created as a class library

### The root folder of the module should be in the modules folder

### The UI is handled by the main [blazor](https://dotnet.microsoft.com/en-us/apps/aspnet/web-apps/blazor) asp.net module